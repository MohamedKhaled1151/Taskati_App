

import 'package:flutter/material.dart';

class ScreanHome extends StatelessWidget {
  const ScreanHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
